{
  "name": "superai-hub",
  "version": "1.0.0",
  "private": true,
  "description": "SuperAI Hub - 50+ AI Helpers & WhatsApp Personal Assistant",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "dev:engine": "npm --prefix whatsapp-engine run dev",
    "dev:all": "concurrently \"npm --prefix web run dev\" \"npm --prefix whatsapp-engine run dev\"",
    "build:web": "npm --prefix web run build",
    "build:engine": "npm --prefix whatsapp-engine run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
